// Placeholder. The three-slide Get Started carousel (why, setup, workflow)
// gets built as its own screen next.
export default function GetStarted() {
    return (
        <div>
            <h1>Get started</h1>
            <p>Welcome to LLM Tuner.</p>
        </div>
    );
}